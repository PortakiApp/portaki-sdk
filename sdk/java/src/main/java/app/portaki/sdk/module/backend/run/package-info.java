/**
 * Orchestration locale des backends modules : contexte de run, pipeline d’étapes, rapport.
 *
 * <p>Réservé à l’exécution dans le classpath du module / de l’hôte — pas de saga durable ni d’intégration
 * avec le bus d’événements de l’application cœur.
 */
package app.portaki.sdk.module.backend.run;
