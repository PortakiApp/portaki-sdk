package app.portaki.sdk.module;

/** Runtime context passed to module handlers (API client, tenant scope, etc.). */
public interface ModuleContext {
  // TODO: expose Portaki API client, tenant id, secrets resolver
}
