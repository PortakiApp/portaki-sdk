/target
**/*.rs.bk

# Cargo.lock est versionné : le workspace publie un binaire (portaki-cli), et la CI
# d'audit lit ce fichier pour savoir quelles versions sont réellement résolues.

# AI agent / Cursor tooling (local only — never commit)
.cursor/
.claude/
.cursorrules
AGENTS.md
CLAUDE.md
.codex/
.continue/
.windsurf/
.aider*
