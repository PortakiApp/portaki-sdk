<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>app.portaki.module</groupId>
  <artifactId>pre-arrival-form-backend</artifactId>
  <version>0.3.0-SNAPSHOT</version>
  <packaging>jar</packaging>
  <name>Pre-arrival form module (Java)</name>

  <properties>
    <java.version>21</java.version>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
  </properties>

  <!-- Résolution du SDK : installer depuis la racine du dépôt avec cd sdk/java && mvn install,
       ou consommer une version publiée sur Maven Central quand disponible. -->

  <distributionManagement>
    <repository>
      <id>github</id>
      <name>GitHub Packages — portaki-modules</name>
      <url>https://maven.pkg.github.com/PortakiApp/portaki-modules</url>
    </repository>
    <snapshotRepository>
      <id>github</id>
      <name>GitHub Packages — portaki-modules (snapshots)</name>
      <url>https://maven.pkg.github.com/PortakiApp/portaki-modules</url>
    </snapshotRepository>
  </distributionManagement>

  <dependencies>
    <dependency>
      <groupId>app.portaki</groupId>
      <artifactId>portaki-module-sdk</artifactId>
      <version>0.3.0-SNAPSHOT</version>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.14.0</version>
        <configuration>
          <release>${java.version}</release>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>
