name: Cancel CI on merge

on:
  pull_request:
    types: [closed]

permissions:
  actions: write

jobs:
  cancel:
    if: github.event.pull_request.merged == true
    uses: PortakiApp/.github/.github/workflows/cancel-ci-on-merge.yml@main
